// array helper
// FIX This method should be in another project - e.g. TheDLibrary already has this.
P.Arrays = {
    torso : function(array) {
        return array.slice(0, array.length - 1);
    }
};
